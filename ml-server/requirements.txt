# CPU-only torch keeps the image small and runs on the ZBook (no CUDA).
# For GPU deploy, swap to a CUDA torch wheel + set ML_DEVICE=cuda.
# torch>=2.6 required: recent transformers refuses torch.load of .bin checkpoints
# (e.g. bge-m3) on older torch for security reasons.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.6.0
fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
pydantic-settings>=2.6
# transformers 5.x: FlagEmbedding's bge-m3 embedder needs it (passes dtype=).
# The reranker is run via transformers directly (below), not FlagReranker, which
# breaks on 5.x — so we stay on one transformers version for both.
transformers==5.12.1
FlagEmbedding>=1.3.0
